# Dependencies (client + server)
node_modules/

# Environment — never commit secrets (templates stay tracked)
.env
.env.*
!.env.example
!**/.env.example

# Build outputs
dist/
dist-ssr/
build/
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Test / coverage
coverage/

# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vite / tooling
*.tsbuildinfo
.cache/

# Server
server/.env
server/drizzle
server/node_modules
server/dist
server/build
server/logs

# Drizzle (optional local artifacts; keep tracked: drizzle/*.sql if you use migrations)
# Uncomment if you generate local SQL you do not want committed:
# drizzle/meta/
